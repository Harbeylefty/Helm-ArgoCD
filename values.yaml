baseImage: "python:3.11-slim"
resources:
  requests:
    memory: "512Mi"
    cpu: "500m"
packages: "numpy,pandas"

service:
  port: 80  # Ensure that the port is specified here
  targetPort: 5000
  type: ClusterIP
