apiVersion: v1
kind: ConfigMap
metadata:
  name: alertmanager-config
data:
  alertmanager.yaml: |
    global:
      resolve_timeout: 5m
    route:
      group_by: ['alertname']
      receiver: 'slack-notifications'
    receivers:
      - name: 'slack-notifications'
        slack_configs:
          - api_url: 'https://hooks.slack.com/services/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'   
            channel: '#all-harbey'   
            send_resolved: true
    templates:
      - '{{ .CommonAnnotations.summary | default "Default summary" }}'
      - '{{ .CommonAnnotations.description | default "Default description" }}'






