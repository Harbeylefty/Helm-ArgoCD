# templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: node-dev-service
spec:
  selector:
    app: node-dev-environment
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}
