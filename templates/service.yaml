apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-dev-environment.fullname" . }}
  labels:
    {{- include "python-dev-environment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-dev-environment.selectorLabels" . | nindent 4 }}
